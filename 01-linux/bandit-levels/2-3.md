# Bandit Level 2 -> 3

## Goal
Log into the Bandit game using SSH. The host is bandit.labs.overthewire.org on port 2220. The username is bandit0 and the password is bandit0.

---

## Thought Process
I listed all files with ls and found `--spaces in this filename--`. Since the filename contains spaces, I needed to escape them using `\` so the command reads it correctly. Another option would be to wrap the filename in quotes. 

---

## Commands Used
```bash
ls
cat ./--spaces\ in\ this\ filename--
```
or 
```bash
cat "./--spaces in this filename--"
```
---

## Result 
Password: MNk8KNH3Usiio41PRUEoDFPqfxLPlSmx

## Next steps
Exit the current SSH session, then connect to bandit2 via SSH on port 2220 using the password you just found.

```bash
exit
ssh bandit3@bandit.labs.overthewire.org -p 2220
```