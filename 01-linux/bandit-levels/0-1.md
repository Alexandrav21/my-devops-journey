# Bandit Level 0 -> 1

## Goal
The next level’s password is in a file named readme in the home directory. Use it to log into bandit1 via SSH. Each time you find a password, use SSH on port 2220 to access the next level and keep going.

---

## Thought Process
Keep using the Killercoda Ubuntu terminal and run `ls` to see what is in the directory. It shows there is only one file called readme. Then run `ls -l`, which lists the contents of a directory in a detailed format, including permissions.

This shows it is a regular file, not a directory. Since it is readable for the current user, the next step is to read it using `cat readme` and get the password.

---

## Commands Used
```bash
ls
ls -l
cat readme
```
---

## Result 
Password: ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If

## Next steps
- Run `exit`, then `ssh bandit1@bandit.labs.overthewire.org -p 2220`, add the new found password and congrats, you made it to the new level!